# Changelog

## 1.6.2+10

- 精简关于页项目简介，并同步清理不再使用的设计参考说明。
- 关于页新增项目仓库：`https://github.com/5Breeze/BambuRFID`。
- 新增完全开源、非商业、不涉及商业利益且不参与商业活动的声明。
- `pubspec.yaml` 增加 repository 元数据。

## 1.6.1+9

- 首页所有视觉元素（Logo、语言切换、耗材图、标题、字体、图标、信息行与间距）在完整适配屏幕后统一额外缩放为上一版的 90%。
- 删除首页缩放算法中的最小缩放下限；短屏设备不再因为被强制放大到最小比例而把底部内容挤出屏幕。
- 首页继续保持无滚动，始终以 SafeArea 的可用宽高完整容纳 360×720 逻辑画布。

## 1.6.0+8

- 修复连续扫描数次后可能出现“手机检测到 NFC、但界面仍停留在旧耗材”的问题。
- 原生 NFC 层改为 latest-tag 串行队列：读取过程中出现的新标签不再被直接丢弃。
- MIFARE Classic 读取增加超时和最多 3 次短重试，降低瞬时 TagLost / I/O 失败。
- 每 4 次事务及读取失败后自动刷新 Android ReaderMode，帮助清理部分机型的 NFC 栈陈旧状态。
- 成功事件新增递增 `scanSequence`，Flutter 即使连续读到相同 UID 也会收到新的完整状态。
- 首页删除 `SingleChildScrollView`，改为固定 360×720 逻辑画布整页缩放。
- 缩放以可用高度为主，窄屏时再受宽度安全上限约束；主页不再上下滑动。
- 标题改为单行自适应缩小，信息行高度略收紧，保证核心内容稳定落在一页内。

## 1.5.0+7

- 极简主界面恢复：白底、大幅耗材图、名称与四行核心信息。
- 颜色增加 `#RRGGBB`，双色标签显示两个 Hex。
- 烘干参数同时显示温度与时间。
- 删除价格字段和价格 UI。
- 重新生成透明线盘 / 动态换色双图层资源，降低原图蓝色塑料偏色。
- 左上角增加可点击 `BambuRFID` 标识，与右侧 `中 / EN` 垂直对齐。
- 首次语言跟随设备语言，用户切换后由 Android SharedPreferences 持久保存。
- 点击 `BambuRFID` 进入关于页面，包含技术、研究参考、版权、许可与商标声明。
- 信息区按最长内容行决定宽度；横线同宽；内容左对齐；信息区整体居中。
- NFC 继续保持无按钮自动读取，新线盘直接替换当前信息。
